import Heading from "../Heading";
import Event from "./Event";

const Events = () => {
  return (
    <div>
      <Heading text="Upcoming Events" />
      <Event />
    </div>
  );
};

export default Events;
