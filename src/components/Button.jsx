import * as React from "react";

const Button = () => {
  return (
    <button className="items-center rounded-full bg-gradient-to-r from-hlg-orange to-hlg-yellow px-12 py-1 text-white font-extrabold text-base font-sans border-solid">
      JOIN US
    </button>
  );
};

export default Button;
