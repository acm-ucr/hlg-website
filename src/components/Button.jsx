import * as React from "react";

const Button = () => {
  return (
    <button className="flex items-center rounded-full bg-gradient-to-r from-hlg-orange to-hlg-yellow px-12 py-1 ml-0 mt-20 text-white font-extrabold text-base font-sans border-solid">
      JOIN US
    </button>
  );
};

export default Button;
